import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vitalsense.dashboard',
  appName: 'VitalSense Dashboard',
  webDir: 'dist'
};

export default config;
